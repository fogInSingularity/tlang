kw := _kw_
id := l(l|d)*
op := op
cnst := dd*.dd* | ' '
str_lit := " "
punc := punc

add ponters, typeof, sizeof
*cring^*

left = blue
right = red

calling convention:
  args throw the stack
  return value on top of the args